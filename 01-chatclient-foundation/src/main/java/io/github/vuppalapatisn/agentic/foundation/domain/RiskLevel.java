package io.github.vuppalapatisn.agentic.foundation.domain;

/** Risk band. Kept as an enum so that untrusted text can never widen the value space. */
public enum RiskLevel {
    LOW,
    MEDIUM,
    HIGH
}
